# Railway Variable References (Auto-populated from services)
# These variables are automatically injected by Railway from linked services

# Imgproxy Service
IMGPROXY_URL="https://${{imgproxy.RAILWAY_PUBLIC_DOMAIN}}"
IMGPROXY_KEY="${{imgproxy.IMGPROXY_KEY}}"
IMGPROXY_SALT="${{imgproxy.IMGPROXY_SALT}}"

# Site Configuration
SITE_URL="https://${{RAILWAY_PUBLIC_DOMAIN}}"

# S3/Storage Bucket Service (bundled-saddlebag)
S3_ACCESS_KEY_ID="${{bundled-saddlebag.ACCESS_KEY_ID}}"
S3_BUCKET="${{bundled-saddlebag.BUCKET}}"
S3_ENDPOINT="${{bundled-saddlebag.ENDPOINT}}"
S3_REGION="${{bundled-saddlebag.REGION}}"
S3_SECRET_ACCESS_KEY="${{bundled-saddlebag.SECRET_ACCESS_KEY}}"

# Legacy bucket config (for backwards compatibility)
BUCKET_ENDPOINT="${{bundled-saddlebag.ENDPOINT}}"
BUCKET_NAME="${{bundled-saddlebag.BUCKET}}"
BUCKET_ACCESS_KEY_ID="${{bundled-saddlebag.ACCESS_KEY_ID}}"
BUCKET_SECRET_ACCESS_KEY="${{bundled-saddlebag.SECRET_ACCESS_KEY}}"
BUCKET_REGION="${{bundled-saddlebag.REGION}}"

# Public URL base for images (virtual-hosted style)
PUBLIC_IMAGE_URL="https://${{bundled-saddlebag.BUCKET}}.${{bundled-saddlebag.ENDPOINT}}"
